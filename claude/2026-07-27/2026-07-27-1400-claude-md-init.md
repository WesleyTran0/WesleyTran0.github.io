# CLAUDE.md init + todo.txt convention

**Prompt:** Read the entire codebase and write a CLAUDE.md (coding style, architecture, tooling).
Also set up a `todo.txt` in tuxedo/todo.txt style — bugs ranked `(A)` with `+bug`, features `(B)`
with `+feature`.

## What I did

- Explored the full repo: `package.json`, configs (`next.config.ts`, `tsconfig.json`,
  `.prettierrc`, `postcss.config.mjs`), `src/lib/*`, `src/app/*`, components, hooks, `globals.css`,
  the deploy workflow, and `design_spec.md`.
- Wrote **`CLAUDE.md`** covering: the SvelteKit → Next.js 15 rewrite, commands (and the absence of
  ESLint/tests), static-export deployment constraints, the filesystem-driven markdown content
  architecture, App Router routing, the PageShell interaction shell, Tailwind v4 CSS-first theming,
  and Prettier rules. Verified it passes `prettier --check`.
- Created **`todo.txt`** and documented the tuxedo convention in CLAUDE.md. Seeded it with the one
  real issue found: the stale SvelteKit `README.md`.

## Notes

- `README.md` was stale SvelteKit boilerplate — logged as an `(A) +bug` item.
